---
# ansible-playbook -i inventory/hosts.yaml runbooks/upgrade_cluster.yaml -u pi --private-key=~/.ssh/ansible -CD
- name: Runbooks | Upgrade Kubernetes on first master node.
  hosts: master[0]
  become: true
  tasks:
    - name: Get package facts.
      ansible.builtin.package_facts:
        manager: apt

    - name: Set the fact to the current kubeadm version.
      ansible.builtin.set_fact:
        current_version: "{{ ansible_facts.packages['kubeadm'][0].version.split('-')[0] }}"
        desired_version: "{{ kubernetes_pkg_version.split('-')[0] }}"

    - name: Сompare the current version with the desired one.
      ansible.builtin.debug:
        msg: "current version: {{ current_version }}, required version: {{ desired_version }}"

    - name: Upgrade cluster.
      when: current_version is version(desired_version, '<', version_type='semver')
      block:
        - name: Only run "update_cache=yes" if the last one is more than 3600 seconds ago.
          ansible.builtin.apt:
            update_cache: true
            cache_valid_time: 3600

        - name: Allow Kubeadm to be upgraded.
          ansible.builtin.dpkg_selections:
            name: "{{ item }}"
            selection: install
          with_items:
            - kubeadm

        - name: Install Kubeadm.
          ansible.builtin.apt:
            name:
              - kubeadm={{ kubernetes_pkg_version }}
            state: present

        - name: Fetching available versions to upgrade.
          ansible.builtin.shell: "kubeadm upgrade plan > cluster_upgrade_v{{ desired_version }}.log 2>&1"
          args:
            chdir: /home/{{ ansible_user }}
            creates: cluster_upgrade_v{{ desired_version }}.log

        - name: Change the cluster version.
          ansible.builtin.shell: "kubeadm upgrade apply v{{ desired_version }} --yes > node_upgrade_v{{ desired_version }}.log 2>&1"
          args:
            chdir: /home/{{ ansible_user }}
            creates: node_upgrade_v{{ desired_version }}.log

        - name: Drain node.
          local_action: ansible.builtin.command kubectl drain {{ ansible_hostname }} --ignore-daemonsets --delete-emptydir-data
          ignore_errors: true
          become: false

        - name: Allow Kubectl, Kubelet to be upgraded.
          ansible.builtin.dpkg_selections:
            name: "{{ item }}"
            selection: install
          with_items:
            - kubelet
            - kubectl

        - name: Install Kubectl, Kubelet.
          ansible.builtin.apt:
            name:
              - kubelet={{ kubernetes_pkg_version }}
              - kubectl={{ kubernetes_pkg_version }}
            state: present

        - name: Prevent Kubeadm, Kubectl, Kubelet from being upgraded.
          ansible.builtin.dpkg_selections:
            name: "{{ item }}"
            selection: hold
          with_items:
            - kubeadm
            - kubelet
            - kubectl

        - name: Restart service Kubectl, also issue daemon-reload to pick up config changes.
          ansible.builtin.systemd_service:
            state: restarted
            daemon_reload: true
            name: kubelet

        - name: Uncordon node.
          local_action: ansible.builtin.command kubectl uncordon {{ ansible_hostname }}
          become: false
