---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: manifests-infra-post-deploy
  namespace: flux-system
spec:
  dependsOn:
    - name: base-infra-deploy
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: "./infra/post-deploy/manifests"
  interval: 60m
  timeout: 5m
  prune: true
  wait: true
